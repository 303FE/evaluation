import actionTypes from '../type'

const initState = {

}

export default function exampleReducer(state = initState, action) {
    switch (action.type) {
        default:
            return state;
    }
}